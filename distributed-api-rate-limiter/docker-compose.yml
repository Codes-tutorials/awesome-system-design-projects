version: '3.8'
services:
  redis:
    image: redis:7.2-alpine
    container_name: redis-distributed-limiter
    ports:
      - "6380:6379" # Using 6380 to avoid conflict with previous project
    command: redis-server --save 20 1 --loglevel warning
